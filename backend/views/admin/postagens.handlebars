<h2>Lista de postagens</h2>
<hr>
<a href="/admin/postagens/add"><button class = "btn btn-success">Nova Postagem</button></a>

{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{titulo}}</h4>
            <h6>Descrição: {{descricao}}</h6><hr>
            <p>Conteúdo: {{conteudo}}</p><hr>
            <small>Slug: {{slug}}</small><br>
            <small>Categoria: {{categoria.nome}}</small><br>
            <small>{{data}}</small>
            <br>
            <a href="/admin/postagens/edit/{{_id}}"><button class="btn btn-success mt-3">Editar Postagem</button></a>
            <form action="/admin/postagens/deletar/{{_id}}" method="POST">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-danger mt-2"> Deletar postagem</button>
            </form>
        </div>
    </div>
{{else}}
    <p>Nenhuma Postagem Realizada!</p>
{{/each}}